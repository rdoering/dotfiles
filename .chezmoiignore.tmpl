README.md

# The Ansible playbook lives in the chezmoi source for versioning but must
# NOT be materialized into ~. It is consumed in-place by
# run_onchange_40_ansible_playbook.sh.tmpl via {{ .chezmoi.sourceDir }}.
ansible
{{- if ne .chezmoi.os "darwin" }}
# Paperless-Upload ist nur auf macOS-Maschinen relevant
.local/bin/paperless-upload.sh
Library/LaunchAgents
{{- end }}
